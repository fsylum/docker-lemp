# Cloudflare Tunnel
# Create a tunnel at https://one.dash.cloudflare.com -> Networks -> Tunnels
# Configure the tunnel's public hostname(s) to point to https://traefik:443
# with "No TLS Verify" enabled under TLS settings.
CLOUDFLARE_TUNNEL_TOKEN=
